Place real product photos here, matching the paths referenced in
lib/data/menu_data.dart (e.g. chicken_momo.jpg, bbq_momo.jpg, etc.).

Until real images are added, product cards render a gradient
placeholder with a food icon instead of a broken image — the app
will still run and look intentional, not broken.
